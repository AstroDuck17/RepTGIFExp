# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ──────────────────────────────────
.venv/
venv/
env/

# ── Local config (contains server-specific paths) ─────────
# Copy config.example.yaml → config.yaml and fill in your paths.
# config.yaml is NEVER committed — each server has its own copy.
# probing/config.yaml

# ── Pipeline artifacts (large binary files) ───────────────
probing/artifacts/

# ── Editor / OS ───────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
